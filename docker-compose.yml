services:
  proxy:
    build: .
    container_name: free-frontier
    restart: unless-stopped
    ports:
      - "4000:4000"
    env_file:
      - path: .env
        required: false
    environment:
      - GEMINI_API_KEY
      - GROQ_API_KEY
      - NVIDIA_NIM_API_KEY
      - OPENROUTER_API_KEY
    volumes:
      - ./config.yaml:/app/config.yaml:ro
